import {
    templateType,
    templateQuery,
    templateMutation,
    templateSubscription,
} from './schema'

const templateSchema = [
    templateType,
    templateQuery,
    templateMutation,
    templateSubscription,
]

export { templateSchema }
