import {
    productType,
    productQuery,
    productMutation,
    productSubscription,
} from './schema'

const productSchema = [
    productType,
    productQuery,
    productMutation,
    productSubscription,
]

export { productSchema }
